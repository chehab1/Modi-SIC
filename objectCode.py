import pandas as pd
import locationCounter
import symbolTable
import rsc.instructionSet as inst_set
from operator import *


# def getObjectCode(df):
#     for i in range(0, len(df['LCounter']) - 1):
